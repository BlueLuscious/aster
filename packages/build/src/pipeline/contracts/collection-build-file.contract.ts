/**
 * @description One generated-root-relative text file crossing the collection build host boundary.
 */
export interface CollectionBuildFile {
  /**
   * @description Canonical generated-root-relative path using `/` separators.
   */
  readonly path: string;

  /**
   * @description Exact UTF-8-compatible text content.
   */
  readonly content: string;
}
