/**
 * @description Immutable built-in icon source formats accepted by Import.
 */
export const iconImportFormats = Object.freeze({
  /** @description Canonical identifier for the svg import format. */
  svg: "svg",
} as const);
