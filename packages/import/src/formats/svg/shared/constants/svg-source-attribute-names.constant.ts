/**
 * @description Immutable namespace-free names used by the accepted and explicitly rejected SVG subset.
 */
export const svgSourceAttributeNames = Object.freeze({
  /** @description Canonical SVG source attribute name for viewBox. */
  viewBox: "viewBox",
  /** @description Canonical SVG source attribute name for namespace declaration. */
  namespaceDeclaration: "xmlns",
  /** @description Canonical SVG source attribute name for version. */
  version: "version",
  /** @description Canonical SVG source attribute name for identifier. */
  identifier: "id",
  /** @description Canonical SVG source attribute name for enable background. */
  enableBackground: "enable-background",
  /** @description Canonical SVG source attribute name for space. */
  space: "space",
  /** @description Canonical SVG source attribute name for path data. */
  pathData: "d",
  /** @description Canonical SVG source attribute name for centre x. */
  centreX: "cx",
  /** @description Canonical SVG source attribute name for centre y. */
  centreY: "cy",
  /** @description Canonical SVG source attribute name for radius. */
  radius: "r",
  /** @description Canonical SVG source attribute name for radius x. */
  radiusX: "rx",
  /** @description Canonical SVG source attribute name for radius y. */
  radiusY: "ry",
  /** @description Canonical SVG source attribute name for x. */
  x: "x",
  /** @description Canonical SVG source attribute name for y. */
  y: "y",
  /** @description Canonical SVG source attribute name for width. */
  width: "width",
  /** @description Canonical SVG source attribute name for height. */
  height: "height",
  /** @description Canonical SVG source attribute name for x1. */
  x1: "x1",
  /** @description Canonical SVG source attribute name for y1. */
  y1: "y1",
  /** @description Canonical SVG source attribute name for x2. */
  x2: "x2",
  /** @description Canonical SVG source attribute name for y2. */
  y2: "y2",
  /** @description Canonical SVG source attribute name for points. */
  points: "points",
  /** @description Canonical SVG source attribute name for fill. */
  fill: "fill",
  /** @description Canonical SVG source attribute name for fill rule. */
  fillRule: "fill-rule",
  /** @description Canonical SVG source attribute name for stroke. */
  stroke: "stroke",
  /** @description Canonical SVG source attribute name for stroke width. */
  strokeWidth: "stroke-width",
  /** @description Canonical SVG source attribute name for stroke line cap. */
  strokeLineCap: "stroke-linecap",
  /** @description Canonical SVG source attribute name for stroke line join. */
  strokeLineJoin: "stroke-linejoin",
  /** @description Canonical SVG source attribute name for stroke miter limit. */
  strokeMiterLimit: "stroke-miterlimit",
  /** @description Canonical SVG source attribute name for opacity. */
  opacity: "opacity",
  /** @description Canonical SVG source attribute name for fill opacity. */
  fillOpacity: "fill-opacity",
  /** @description Canonical SVG source attribute name for stroke opacity. */
  strokeOpacity: "stroke-opacity",
  /** @description Canonical SVG source attribute name for transform. */
  transform: "transform",
  /** @description Canonical SVG source attribute name for href. */
  href: "href",
  /** @description Canonical SVG source attribute name for source. */
  source: "src",
} as const);
