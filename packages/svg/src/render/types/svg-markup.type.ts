/**
 * @description Complete standalone SVG markup produced atomically by the SVG renderer.
 * @remarks The value remains a plain string and grants no browser insertion or trusted-markup
 * authority.
 */
export type SvgMarkupType = string;
