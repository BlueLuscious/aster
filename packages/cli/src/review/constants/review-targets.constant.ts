/**
 * @description Immutable output targets accepted by headless review planning.
 */
export const reviewTargets = Object.freeze({
  /** @description Self-contained HTML review-document target. */
  html: "html",
} as const);
