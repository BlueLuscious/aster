/**
 * @description Immutable failure families owned by standalone export publication.
 */
export const exportOutputErrorKinds = Object.freeze({
  conflict: "conflict",
  failure: "failure",
} as const);
