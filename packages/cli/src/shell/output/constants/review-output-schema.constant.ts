/**
 * @description Immutable standalone publication defaults for static review documents.
 */
export const reviewOutputSchema = Object.freeze({
  /** @description Default directory used for static review publication. */
  defaultRoot: "aster-review",
} as const);
