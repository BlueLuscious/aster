/**
 * @description Acquired workspace package identity, manifest, and filesystem root.
 * @typedef {object} TWorkspacePackageRecord
 * @property {string} directory - Workspace-relative package directory name.
 * @property {string} name - Canonical workspace package identity.
 * @property {Record<string, unknown>} manifest - Parsed package manifest.
 * @property {string} packageRoot - Absolute package root path.
 */

export {};
