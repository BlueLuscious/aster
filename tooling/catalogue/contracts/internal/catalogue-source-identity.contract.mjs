/**
 * @description Logical identity and symbol derived from one canonical source path.
 * @typedef {object} ICatalogueSourceIdentity
 * @property {string} name - Canonical glyph or collection name.
 * @property {string | undefined} variant - Optional explicit icon rendition.
 * @property {string} symbol - Exact filename-owned definition export symbol.
 */

export {};
