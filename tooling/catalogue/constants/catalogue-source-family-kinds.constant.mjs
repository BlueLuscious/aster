/**
 * @description Immutable catalogue source-family discriminators.
 */
export const catalogueSourceFamilyKinds = Object.freeze({
  /** @description Family containing canonical icon definitions. */
  icon: "icon",
  /** @description Family containing canonical collection definitions. */
  collection: "collection",
});
