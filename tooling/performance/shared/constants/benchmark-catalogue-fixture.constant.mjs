/**
 * @description Immutable scale authority for the shared synthetic benchmark catalogue.
 */
export const benchmarkCatalogueFixture = Object.freeze({
  /** @description Stable number of definitions represented by collection-scale scenarios. */
  corpusSize: 16,
});
