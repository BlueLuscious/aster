/**
 * @description Immutable package paths and emitted-file suffixes used by distribution inspection.
 */
export const packageDistribution = Object.freeze({
  /** @description Emitted TypeScript declaration filename suffix. */
  declarationSuffix: ".d.ts",
  /** @description Package manifest filename. */
  manifest: "package.json",
  /** @description Emitted JavaScript module filename suffix. */
  moduleSuffix: ".js",
  /** @description Direct package distribution directory name. */
  outputDirectory: "dist",
});
