/**
 * @description Prepared public Core values used outside benchmark timing boundaries.
 * @typedef {object} ICoreBaselineFixtures
 * @property {readonly import("@aster/core").IconDefinition[]} canonicalIcons - Canonical frozen pilot icons.
 * @property {readonly import("@aster/core").IconDefinition[]} mutableIcons - Structurally equivalent mutable icon inputs.
 * @property {import("@aster/core").CollectionDefinition} canonicalCollection - Complete canonical frozen pilot collection.
 * @property {import("@aster/core").CollectionDefinition} mutableCollection - Structurally equivalent mutable complete collection input.
 * @property {import("@aster/core").CollectionDefinition} emptyCollection - Mutable valid collection without members.
 * @property {import("@aster/core").CollectionDefinition} singleCanonicalCollection - Mutable collection retaining one canonical member candidate.
 */

export {};
