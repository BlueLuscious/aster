/**
 * @description Host-neutral directory entry exposed to private repository tooling.
 * @typedef {object} IRepositoryDirectoryEntry
 * @property {string} name - Immediate entry name without its parent path.
 * @property {import("../../types/internal/repository-entry-kind.type.mjs").TRepositoryEntryKind} kind - Filesystem entry classification.
 */

export {};
